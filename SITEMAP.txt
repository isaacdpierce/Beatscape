App
  - Header
    - Navbar
  - Main
    - Machine
      - Track
          - TrackSlider
            - input

REFACTOR:

present:

Machine 
      <TopControls />
      <Drawer />

TopControls
      <Button/>
      <Button/>

Tracks
    Track * 12 inputs

Drawer 
    Button - openDrawer()


NEED:

const style 1
const style 2

  Machine
    open -> setOpen -> useState('false')
    handleClick(() => setOpen(!open))
      <TopControls />
      <Tracks/>
          <Track/>
      <BottomControls handleClick={handleClick}/>
        <Button handleClick={handleClick}/>
        <Effect slider />
        <Effect slider />
      <Drawer style={open}/>
         
